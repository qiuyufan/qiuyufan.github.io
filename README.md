# qiuyufan.github.io

Personal portfolio, served via GitHub Pages. Plain HTML/CSS, no build step.

- `index.html` — page content
- `css/style.css` — styling

To add a project, duplicate a `<article class="card">` block in the
"Projects" section of `index.html`.